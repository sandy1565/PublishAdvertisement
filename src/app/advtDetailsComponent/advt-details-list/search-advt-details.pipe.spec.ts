import { SearchAdvtDetailsPipe } from './search-advt-details.pipe';

describe('SearchAdvtDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchAdvtDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
